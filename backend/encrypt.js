export function encrypt(plaintext, keys) {
  // this function will encrypt the file and return a string variable with the ciphertext
  // the plaintext parameter is a string of the plain text
  // the keys parameter is an array with all of the keys
  return `${plaintext} this is from the backend`
}